Once a String object has been created, you cannot change the characters that comprises the String.
It will create a new object if you try to do so.

For those cases in which a modifiable String is required, Java provides two options: StringBuffer and StringBuilder.